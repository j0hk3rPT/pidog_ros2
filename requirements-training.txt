# PiDog Training Requirements
# Install with: pip3 install -r requirements-training.txt

# Core ML dependencies (CPU-only PyTorch)
# For GPU support, see requirements-gpu.txt

# PyTorch CPU version (default)
# Uncomment ONE of the following based on your setup:

# === CPU Only (default) ===
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# === For NVIDIA GPUs (CUDA 12.1) ===
# Install with: pip3 install -r requirements-gpu-cuda.txt

# === For AMD GPUs (ROCm 6.2) ===
# Install with: pip3 install -r requirements-gpu-rocm.txt

# Visualization and data processing
matplotlib>=3.5.0
numpy>=1.21.0

# Optional: Advanced training features
tensorboard>=2.10.0

# Optional: Better progress bars
tqdm>=4.65.0
