# PiDog Training Requirements - AMD GPU (ROCm)
# Install with: pip3 install -r requirements-gpu-rocm.txt

# PyTorch with ROCm 6.2 support
--extra-index-url https://download.pytorch.org/whl/rocm6.2
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Visualization and data processing
matplotlib>=3.5.0
numpy>=1.21.0

# Optional: Advanced training features
tensorboard>=2.10.0
tqdm>=4.65.0

# Note: ROCm has specific GPU requirements
# Supported: RX 7900 XTX/XT, RX 7800 XT, RX 7700 XT, RX 7600
# For older GPUs or installation issues, use Docker instead:
#   docker-compose --profile rocm up -d pidog-rocm

# Verification:
# After installation, run:
#   python3 -c "import torch; print(f'ROCm available: {torch.cuda.is_available()}'); print(f'GPU: {torch.cuda.get_device_name(0)}')"
